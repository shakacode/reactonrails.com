#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Install dependencies.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
npm ci
npm --prefix prototypes/docusaurus ci
